node_modules/
dist/
.next/
*.tsbuildinfo
.env
.env.local
.DS_Store

# Runtime-uploaded content (posters, logos) — not source, regenerable via
# the admin UI. Keep the directories themselves via .gitkeep.
backend/uploads/*
!backend/uploads/.gitkeep
!backend/uploads/*/.gitkeep

# Scratch space for server-side media conversion
work/
backend/up72.mjs
backend/fix72.sh
yt/

# Store screenshots are large binaries generated from device captures.
brand/screenshots/
